{
  "name": "jevalis",
  "version": "1.0.0",
  "description": "Jevalis — Sovereign AI & Digital Infrastructure",
  "private": true,
  "scripts": {
    "build": "echo 'Static site — no build step required'",
    "start": "npx serve ."
  }
}
